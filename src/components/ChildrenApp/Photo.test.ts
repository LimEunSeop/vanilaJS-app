describe('Photo App', () => {
  it('사진 리스트가 올바르게 보이고 동작하는가?', () => {
    // 사진 src, alt 가 제대로 들어갔는가?
    // 사진 하나 클릭 시 해당 요소에  selected 클래스가 생기고 나머지 요소는 제거되는가?
  })

  it('사진 하나 선택하면 아래에 제대로 표시되는가?', () => {})
})
