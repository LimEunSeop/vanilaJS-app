describe('Alarm App', () => {
  it('알람 추가 폼이 제대로 나오고 동작하는가?', () => {
    // 필수 요소들이 제대로 렌더링 되는가?
    // 분은 10분 단위로 입력받도록 렌더링 되는가?
    // 저장 클릭시 item 하나가 추가 되고 추가 폼이 사라지는가?
    // 로컬스토리지에 반영되는가?
  })

  it('알람 리스트에 아이템이 제대로 나오고 동작하는가?', () => {
    // 알람이 제대로 렌더링 되는가?
    // 삭제버튼 클릭 시 아이템 하나가 지워지는가?
    // 로컬스토리지에 반영되는가?
  })
  it('알림시간이 되면 alert 호출되고 알람이 삭제되는가?', () => {})

  it('로컬스토리지의 데이터를 제대로 로드하는가?', () => {})
})
