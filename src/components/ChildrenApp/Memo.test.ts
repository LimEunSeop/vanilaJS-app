describe('Memo App', () => {
  it('메모 추가 폼이 제대로 나오고 동작하는가?', () => {
    // 필수 요소들이 제대로 렌더링 되는가?
    // 엔터 입력시 저장되고 입력창 사라지는가?
    // 로컬스토리지에 저장되는가?
  })
  it('메모 리스트에 아이템이 제대로 나오고 동작하는가?', () => {
    // 메모가 제대로 들어가는가?
    // 메모 클릭 시 active 클래스가 추가되고 나머지 아이템은 active 클래스 사라지는가?
  })

  it('로컬스토리지의 데이터가 제대로 로드되는가?', () => {})
})
